-- This migration cannot be easily reversed as we've deleted duplicate data
-- If you need to roll back, restore from a backup
SELECT 'Migration 004 cannot be automatically reversed - restore from backup if needed';
